map key with xkb
	capslock -> lshift
		key <CAPS> {    [ Shift_L       ]   };
	lshift -> lctrl
		key <LFSH> {    [ Control_R     ]   };
		// key <LFSH> {    [ Control_L     ]   }; // left not working
